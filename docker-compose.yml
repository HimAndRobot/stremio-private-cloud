services:
  stremio-private-cloud:
    build: .
    ports:
      - "11780:11780"
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
